import React from "react";
import type { SearchBarProps } from "./Type";

interface props {
  data: SearchBarProps;
}

function SearchInput({ data }: props) {
  return (
    <div>
    </div>
  );
}

export default SearchInput;
